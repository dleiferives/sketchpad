#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

SKETCHPAD_REMOTE_HOST="${SKETCHPAD_REMOTE_HOST:-atlas}" \
SKETCHPAD_ZELLIJ_TAB="${SKETCHPAD_ZELLIJ_TAB:-atlas}" \
  exec "$script_dir/zellij-remote" "$@"
